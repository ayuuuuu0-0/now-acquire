import 'package:flutter/material.dart';

class StartupHomeScreen extends StatelessWidget {
  const StartupHomeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Startup homescreen"),
      ),
    );
  }
}
